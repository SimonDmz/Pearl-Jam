import D from 'i18n';

const toDoEnum = {
  CONTACT: `${D.toDoContact}`,
  SURVEY: `${D.toDoSurvey}`,
  FINALIZE: `${D.toDoFinalize}`,
  TRANSMIT: `${D.toDoTransmit}`,
  SYNCHRONIZE: `${D.toDoSynchronize}`,
};

export default toDoEnum;
